# Database configuration for counselrank.legal
# Copy this content to .env.local and update with your actual credentials

# PostgreSQL connection string
DATABASE_URL="postgresql://username:password@host:port/database_name?serverVersion=16&charset=utf8"

# Example:
# DATABASE_URL="postgresql://myuser:mypassword@192.168.1.100:5432/tulsa_seo?serverVersion=16&charset=utf8"

# Google OAuth Configuration
GOOGLE_OAUTH_CLIENT_ID="your_google_oauth_client_id"
GOOGLE_OAUTH_CLIENT_SECRET="your_google_oauth_client_secret"
GOOGLE_OAUTH_REDIRECT_URI="http://localhost:8000/api/v1/auth/google/callback"

# Microsoft OAuth Configuration
MICROSOFT_OAUTH_CLIENT_ID="your_microsoft_oauth_client_id"
MICROSOFT_OAUTH_CLIENT_SECRET="your_microsoft_oauth_client_secret"
MICROSOFT_OAUTH_REDIRECT_URI="http://localhost:8000/api/v1/auth/microsoft/callback"

# Frontend URL
APP_FRONTEND_URL="http://localhost:3000"

# Update these values:
# - username: your database username
# - password: your database password  
# - host: your database host/IP address
# - port: your database port (usually 5432)
# - database_name: your database name
# - GOOGLE_OAUTH_CLIENT_ID: your Google OAuth client ID from Google Cloud Console
# - GOOGLE_OAUTH_CLIENT_SECRET: your Google OAuth client secret from Google Cloud Console
# - GOOGLE_OAUTH_REDIRECT_URI: your OAuth callback URL (should match Google Cloud Console)
# - APP_FRONTEND_URL: your frontend application URL
